# 本仓库工程规范

## 每次提交必须检查

```text
功能正确
命名清晰
职责明确
输入已校验
边界已处理
异常可追踪
测试可复现
当前限制已说明
```

## 当前模块规则

- 练习项目可以保持简单，不提前引入复杂架构。
- 不为了“看起来高级”堆抽象。
- 业务规则不要全部堆在`Program.cs`。
- 修Bug后必须重新测试相关场景。
- 代码能运行不等于工程验收通过。

## 提交前检查

```powershell
dotnet build
dotnet test
git status
```
